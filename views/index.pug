<!DOCTYPE html>
html(lang="en")
head
  meta(charset="UTF-8")
  title Express Gallery
  link(href='https://fonts.googleapis.com/css?family=Oswald' rel='stylesheet' type='text/css')
  link(rel="stylesheet" href="/css/index.css")
body
  div(class="header") Welcome to my Gallery!
  div(class="homeButtons")
    div
      form(method="POST" action="/gallery?_method=GET")
        button(type="submit") Home
    div
      form(method="POST" action="/gallery/new?_method=GET")
        button(type="submit") Add Photo
  div(class="gallery")
    each val, index in entries
      div(class="image" style="background-image: url('" + val.url + "')")
        div
          form(method="POST" action="/gallery/" + val.id + "?_method=GET")
            button(style="background-color: transparent; border: none")
        div(class="author") Uploaded by: #{val.author}